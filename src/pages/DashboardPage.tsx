function DashboardPage() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
        fontSize: "40px",
        fontWeight: "bold"
      }}
    >
      Dashboard Working
    </div>
  );
}

export default DashboardPage;